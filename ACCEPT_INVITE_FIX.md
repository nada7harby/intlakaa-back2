# ✅ تم إصلاح Accept Invite Endpoint

## 🔧 التغييرات:

### **1. إضافة Routes إلى authRoutes.js:**
```javascript
router.get('/verify-invite', verifyInvite);
router.post('/accept-invite', acceptInvite);
```

### **2. تحديث verifyInvite:**
- الآن يستخدم `AdminInvite` model بدلاً من `Admin.inviteToken`
- يبحث عن الدعوة في collection `adminInvites`
- يتحقق من أن الدعوة لم تُقبل بعد

### **3. تحديث acceptInvite:**
- يبحث عن الدعوة في `AdminInvite` model
- يتحقق من صلاحية الـ token
- ينشئ admin جديد أو يحدث الموجود
- يضع علامة على الدعوة كـ "مقبولة"
- يُرجع JWT token للتسجيل التلقائي

---

## 🚀 الخطوات التالية:

### **1. أعد تشغيل السيرفر:**
```bash
# Ctrl + C لإيقاف السيرفر
npm run dev
```

### **2. جرب قبول الدعوة:**
1. افتح رابط الدعوة من البريد الإلكتروني
2. أدخل كلمة المرور
3. اضغط "تعيين كلمة المرور"

---

## 📋 API Endpoints المتاحة الآن:

### **POST /api/auth/send-invite**
إرسال دعوة جديدة

### **GET /api/auth/verify-invite?token=xxx**
التحقق من صلاحية token الدعوة

### **POST /api/auth/accept-invite**
قبول الدعوة وتعيين كلمة المرور

Request:
```json
{
  "token": "abc123...",
  "password": "your_password",
  "name": "Admin Name" // اختياري
}
```

Response:
```json
{
  "success": true,
  "message": "Password set successfully",
  "token": "jwt_token_here",
  "admin": {
    "id": "...",
    "name": "Admin Name",
    "email": "admin@example.com",
    "role": "admin"
  }
}
```

---

## ✨ الآن النظام يعمل بالكامل!

1. ✅ إرسال الدعوات
2. ✅ استلام البريد الإلكتروني
3. ✅ التحقق من الدعوة
4. ✅ قبول الدعوة وإنشاء الحساب
5. ✅ تسجيل الدخول التلقائي

**فقط أعد تشغيل السيرفر وجرب!** 🎉
