# 🔧 حل مشكلة بيانات اعتماد Gmail

## ⚠️ المشكلة:
السيرفر يحاول إرسال بريد إلكتروني لكن بيانات اعتماد Gmail غير صحيحة.

---

## ✅ الحل السريع (وضع التطوير):

### **الخطوة 1: افتح ملف .env**
في مجلد `backend`، افتح ملف `.env`

### **الخطوة 2: عطّل إعدادات البريد**
ضع علامة `#` قبل سطور البريد الإلكتروني:

```env
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=2043.live2050@gmail.com
# EMAIL_PASSWORD=your_password_here
# EMAIL_FROM=noreply@intlakaa.com
```

### **الخطوة 3: احفظ الملف**

### **الخطوة 4: أعد تشغيل السيرفر**
```bash
# أوقف السيرفر: Ctrl + C
npm run dev
```

---

## 🎯 النتيجة المتوقعة:

بعد إعادة التشغيل، عند إرسال دعوة ستحصل على:

```json
{
  "success": true,
  "message": "Invite created successfully (Email not configured - Development Mode)",
  "data": {
    "email": "test@example.com",
    "token": "...",
    "inviteUrl": "http://localhost:8080/accept-invite?token=..."
  }
}
```

وفي console السيرفر:
```
⚠️  Email not configured. Running in development mode.
📧 Invite URL: http://localhost:8080/accept-invite?token=...
```

---

## 📧 (اختياري) إصلاح إعدادات Gmail:

إذا أردت إرسال بريد إلكتروني حقيقي لاحقاً:

### **1. فعّل المصادقة الثنائية في Google:**
- اذهب إلى: https://myaccount.google.com/security
- فعّل "2-Step Verification"

### **2. أنشئ App Password:**
- اذهب إلى: https://myaccount.google.com/apppasswords
- اختر "Mail" و "Windows Computer"
- انسخ كلمة المرور المكونة من 16 حرف

### **3. استخدم App Password في .env:**
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=2043.live2050@gmail.com
EMAIL_PASSWORD=xxxx xxxx xxxx xxxx  # App Password من Google
EMAIL_FROM=noreply@intlakaa.com
```

---

## 🚀 الحل الموصى به الآن:

**عطّل إعدادات البريد واستخدم وضع التطوير!**

هذا أسرع وأسهل للتطوير والاختبار. يمكنك تفعيل البريد لاحقاً في الإنتاج.

---

**الخطوات:**
1. ✅ افتح `.env`
2. ✅ ضع `#` قبل سطور EMAIL_*
3. ✅ احفظ
4. ✅ أعد تشغيل السيرفر
5. ✅ جرب مرة أخرى!
